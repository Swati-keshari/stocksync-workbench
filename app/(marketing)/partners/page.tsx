import { CopyPage } from "@/components/site/CopyPage";

export default function Page() {
  return (
    <CopyPage
      title="Partners"
      lead="Placeholder logos would live here in a real company site."
      paragraphs={[
        "Till vendors, warehouse scanners, and commerce platforms would be listed.",
        "This demo only names the three mock sources inside the workbench.",
      ]}
    />
  );
}
